{# {% extends "_layout.jinja2" %} #}
{% block content %}

{% for message in get_flashed_messages() %}
    <div class=flash>{{ message }}</div>
{% endfor %}

{% if not session.login %}
<a href="{{ url_for('connect') }}">log in</a>
{% else %}
<<<<<<< HEAD
<a href="{{ url_for("disconnect") }}">log out</a>
<a href="{{ url_for("account") }}">my account</a>
<a href="{{ url_for("my_snippets", person_id = session.id) }}">my snippets - {{ session.login }}</a>


{% endif %}

<h4>Les plus récents</h4>
<ul class="index">
{% for item in snippets %}
  <li>
    <a href="{{url_for("get_snippet_by_id", snippet_id = item.id)}}">{{ item.title }}</a>
  </li>
{% endfor %}
</ul>

{% endblock content %}
